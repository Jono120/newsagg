# Frontend UI (newsagg-ui) is provisioned in infra/main.bicep and deployed by .github/workflows/newsagg-ui.yml
name: newsagg-dev

infra:
  path: infra

services:
  backend:
    project: backend/NewsAggregator.Api
    host: appservice
    language: dotnet

  scraper:
    project: functionapp
    host: function
    language: python
